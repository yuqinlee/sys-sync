# =============================================================================
#                       导入通用 shell 环境
# =============================================================================
IMPORT_PRE="$HOME/.config/.common/shell/import-prev.sh"

if [ -s "$IMPORT_PRE" ]; then
    . "$IMPORT_PRE"
fi
